# dev4-review-workspace — 팀 안내 (Teams 공유용, 2026-09-17)

CUBRID PR 을 **Claude Code 세션으로 리뷰**하기 위한 스킬·규칙·하네스 리포. 코드를 고치는 곳이 아니라 **코멘트를 만들어 게시하는 곳**이다. 리뷰어 계정으로 게시되므로, 게시 전 승인은 사람이 한다.

## 1. 이 세션으로 리뷰하는 법 (5분)

```bash
git clone https://github.com/soheejung-cs/dev4-review-workspace ~/dev/docs/dev4-review-workspace
ln -sfn ~/dev/docs/dev4-review-workspace/skills/harness-review ~/.claude/skills/harness-review
pip3 install --user jsonschema pyyaml tree_sitter\<0.21     # tree-sitter 문법 .so 는 harness/README.md 참고, 없으면 ctags 로 자동 대체
python3 --version                                           # 3.7 이상이어야 한다(dataclasses). 3.6 이면 python3.11 등으로 위 pip 과 아래 명령을 돌린다
gh auth status                                              # GitHub CLI 로그인 (PR 읽기·코멘트 게시)
```

Claude Code 를 `~/dev/docs/dev4-review-workspace` 에서 열고:

```
/harness-review 7937            ← PR 번호. 제목·브랜치·JIRA 키를 주면 gh 로 찾아준다
```

세션이 하는 일 (사람은 마지막 단계만):
1. `python3 -m tools.harness.run full --pr 7937` — PR diff 를 받아 변경 함수 주변의 **코드 그래프·호출 도달성·불변조건·성능 규칙**을 한 묶음(`review_request*.md`)으로 만든다. LLM 은 이 묶음만 읽는다(저장소 임의 탐색 금지 → 토큰·환각 억제).
2. LLM 이 지적을 `findings.json` 으로 쓴다. **모든 지적에 필수**: 왜 문제인지(`why`), 제안+예시(`proposal`), 근거 줄(`evidence`), 카테고리, 중요도, 에러 우려면 검증(static/dynamic) 결과.
3. `run full --findings` — 스키마 검증, 앵커가 diff 안에 있는지, 근거 줄이 실제 존재하는지, 규칙 ID 가 규칙집에 있는지, 에러 주장에 검증이 붙었는지를 **기계가 판정**한다. 어긋난 항목은 `requery.json` 으로 한 번 재검토시킨다.
4. 사람이 "N번 코멘트 — [층] [카테고리] 이모지 본문" 초안을 보고 승인 → **인라인 코멘트로만** 게시(보고서 본문 게시 안 함). 첫 줄 형식은 `[설계 리뷰] [설계 판정] 🟡` 처럼 층·카테고리·중요도(🔴 blocking/확인된 버그, 🟡 중간, 🟢 주석·문서).

게시 예시: CUBRID/cubrid PR #7899(ALTER INDEX COMPACT), #7937(복합 FK 선택도), #7658 의 인라인 코멘트가 이 절차로 나왔다. 초안·보고서 예시는 `examples/`.

### 구현·설계 쪽 (2026-09-17 추가)
- `/design-doc <CBRD-n|증상>` — 증상 인터뷰(5라운드, 닫힌 질문) → `projects/CBRD-n/설계문서.md`. 영역 판정은 `implement_pack` 이 만든 후보 함수·관문 경로를 근거로 확인받는다.
- `/harness-implement <CBRD-n>` — `tools.harness.implement_pack`(심볼·키워드 → 후보 함수 팩) → `plan.json`(스키마 `harness/schemas/plan.json`, **승인 지점**) → 구현 → `tools.harness.implement_gate`(범위·codestyle·-fsyntax-only·관문 짝) → `/harness-review` 로 자기 리뷰 → draft PR(**승인 지점**).

## 2. 하네스는 어떻게 만들었나

**출발점 두 개를 합쳤다**: ARM **Metis**(tree-sitter 코드 그래프 + 도달성 분석으로 "LLM 이 읽을 것"을 기계가 고름)와 Azure **Architecture Review Agent**(변경으로부터 계층·의존 그래프를 유추해 리스크와 다이어그램을 냄). 둘 다 공통 원칙은 *LLM 에게 저장소를 뒤지게 하지 말고, 결정론적 도구가 컨텍스트를 만들고 LLM 산출물을 도구가 다시 판정한다*.

| 층 | 구현 (`tools/harness/`) | 하는 일 |
|---|---|---|
| CodeGraph | `codegraph.py` | tree-sitter(C/C++ 문법 .so)로 함수·호출·전역 접근을 SQLite 에 저장. 파일 지문 캐시로 재실행 수 초. tree-sitter 없으면 ctags 대체 |
| Reachability | `reachability.py` | 변경 함수 → 게이트(latch·lock·log·vacuum) 경로, 진입점 → 변경 함수 경로, latch 짝 검사 |
| Context pack | `context_pack.py` | 변경 함수 본문 + 호출자/피호출자 + 모듈 AGENTS.md 불변조건 + 규칙집 해당 장 + 과거 코멘트(episodic) 를 예산 안에서 우선순위대로 배치. 함수 단위 배치 분할 |
| Arch infer | `arch_infer.py` | 변경 엣지만으로 계층 사이클·역전·게이트 불균형·latch-then-lock·핫 공유 리스크 산출, mermaid 다이어그램 |
| Perf claims | `perf_claims.py` | PR 본문의 성능 주장을 MEAS 규칙(측정 반복·환경·단위)으로 자동 판정해 "측정 요청" 지적 생성 |
| Adjudicate | `adjudicate.py` | finding 스키마 검증 + 의무 8종(앵커 존재·diff 내·근거 해석·규칙 ID·그래프 지지·why·proposal·에러면 검증) + 중복 제거 + 중요도 채움 + 코멘트 헤더 |
| Pipeline | `pipeline.py`, `harness/pipelines/*.yaml` | 단계 그래프를 YAML 로 선언, 매니페스트에 파이프라인·규칙·스킬 해시와 코드그래프 지문을 기록(재현성) |
| Memory | `episodic.py` | 게시된 코멘트를 PR 별로 회수해 다음 팩에 "이미 지적한 것" 으로 넣음 |

**규칙·스킬이 하네스의 절반이다**: `rules/`(성능·설계 리뷰 규칙), `references/C-Cpp-성능규칙집.md`(ID 로 인용), `skills/*`(code-review · design-review · review-response · review-testing · oracle-reference · record). 하네스는 이 문서들을 그대로 팩에 넣고, 판정기는 규칙 ID 가 여기 있는지 확인한다.

**만든 순서**: v1(리뷰만, 자체 채점 2.4/4) → 4 Primitives(컨텍스트 선별·산출물 계약·판정·기억) 기준으로 갭을 메워 v2(3.9/4) → 리뷰·설계·성능을 `full` 한 명령으로 통합(v2.1). 채점 근거와 남은 한계는 `harness/META-REVIEW.md`, 코드 패턴은 `harness/ARCHITECTURE.md`.

**남은 한계(정직하게)**: 지적 자체는 LLM 이 쓴다 — 하네스는 *근거 없는 지적*을 걸러 줄 뿐 *놓친 지적*은 못 잡는다. 동적 검증(빌드·재현)은 사람이 승인해야 돈다.

## 3. 여기에 없는 것

- **Oracle 매뉴얼 원문** — 재배포 금지라 이 공개 리포에는 없다. private `soheejung-cs/dev4-oracle-manual` (초대 후 clone) 또는 내부망 http://192.168.6.51:8827/docs/dev4-oracle-manual/ . 위치 안내 `references/oracle-manual/README.md`, 읽는 규약 `skills/oracle-reference`(요청 시에만, 목차로 절을 찍어 그 쪽만).
- **비공개 TC 인덱스·TC 분석 도구** — private `dev4-tc-workspace`.
- **소스 분석 지식**(모듈별 §1~§4) — public `dev4-ai-source`. 하네스가 팩에 끌어 쓴다.

## 4. 게시 규약 요약 (`skills/review-response`)

- 인라인만. 첫 줄 `[코드 리뷰|설계 리뷰] [카테고리] 🔴/🟡/🟢`.
- 본문: 지적 → `왜 문제가 되는지:` → `제안:`(주석이면 ```suggestion 블록, 코드는 스케치, 테스트는 TC 시나리오) → `검증(static|dynamic):`.
- 에러 우려(데드락·크래시·오답)는 확인 전엔 질문형으로.
- 테스트 매트릭스는 **제안**이며 돌릴지는 요청자에게 확인한다(`skills/review-testing`).
