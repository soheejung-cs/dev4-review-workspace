# 두 리포 정리 계획 (dev4-review-workspace ↔ claude-workspace)

조사·감사: Claude 세션 `017zfELL` 2026-09-21(`.51`). 워킹트리 실측 기준.
**이 문서는 남은 작업 목록이다.** 끝난 항목은 지우지 말고 ✅ 로 표시한다.

## 분할 원칙 — 한 문장

> **팀 리포에는 "PR 을 리뷰하는 방법"만 둔다.** 그 방법이 돌기 위해 필요한 *내 기계·내 계정·내 산출물*은
> 개인 리포이거나 `members/<이름>/` 이고, 팀 리포는 그것들을 **이름으로만** 안다(값은 env·roster·인자로 받는다).

판정표 — 새 파일을 어디 둘지. **먼저 Yes 가 나오는 줄이 답이다.**

| # | 질문 | Yes → |
|---|---|---|
| 1 | 팀원이 이 리포만 clone 해서 `/harness-review <PR>` 를 돌릴 때 **없으면 안 되나** | **팀** (`skills/`·`rules/`·`references/`·`harness/`·`tools/`) |
| 2 | 컨테이너 번호·IP·`~/bin/*`·설치본 이름·개인 GitHub/JIRA ID 가 **본문에 박혀야** 하나 | **팀 `members/<이름>/`** (CONTRIBUTING §3) |
| 3 | 특정 JIRA 이슈 하나에만 의미가 있나 | **개인 `projects/CBRD-*/`** |
| 4 | 세 컨테이너 공통의 *일하는 방식*인데 리뷰 밖(빌드·측정·보드·보고)에도 쓰이나 | **개인 `memory/rules\|howto/`** |
| 5 | 리뷰에서만 쓰이는 규약인가 | **팀 `skills/*/SKILL.md`** (스킬이 곧 규약 — CONTRIBUTING §4) |

**한 사실은 한 곳.** 다른 쪽이 필요하면 한 줄 포인터만 쓴다 — 본문 재서술 금지.
**팀→개인 방향 링크 금지.** 개인 리포는 프라이빗이라 팀원이 못 연다. 필요하면 **사실을 팀 리포 안으로 복사**한다.

## 끝난 것 ✅

- ✅ `pipeline.py`·`review_board_gen.py` 의 `~/dev/docs/claude-workspace` 하드코딩 제거 → `records_root()`(env `DEV4_RECORDS_ROOT` → `roster.json:records_root` → 기본값). 커밋 `eaf9be2`.
- ✅ 같은 커밋에서 **조용한 강등** 수정 — 경로가 없을 때 blocking 을 전부 non-blocking 으로 떨구던 것을 "평가 불가" 로 갈랐다.
- ✅ 팀 `README.md` 의 `(구)` 두 줄 삭제(내부망 IP `192.168.1.48` 이 팀 README 에 남은 유일한 자리였다) + `tests/shell-tc-index/` 유령 줄 삭제.
- ✅ 추적 중이던 `tools/__pycache__/*.pyc` 3개 `git rm --cached`(`.gitignore` 는 이미 맞았다).
- ✅ `skills/review-testing` 의 프라이빗 문서 절 인용(`테스트TC-규칙 §6`) 제거 — 사실을 본문으로 옮기고 로케일 함정을 덧붙였다.
- ✅ 게시 규약 두 개를 팀으로: `CONTRIBUTING` §8(참조 DBMS 제품명 금지) · §9(메일·메시지 대리 발송 금지).
- ✅ `README.md` 에 스킬 목록 절 신설 — 11개 + 포인터 2개, 등록 방법, 리포 밖 전제.

## 남은 것 — 위험 순

### A. 팀 → `members/정소희/` (CONTRIBUTING §3 위반 해소)

| 대상 | 가야 할 곳 | 위험 |
|---|---|---|
| `skills/review-testing/SKILL.md` §2 의 JOB 하네스 5종(`setup_arm_51.sh` 등)·`~/bin/host_idle_guard.sh`·`goto`·"JOB 은 .51 / TPC-H 는 .50" | `members/정소희/측정환경.md` 신설, 스킬엔 "실행 환경은 `members/<요청자>/`" 한 줄 | 낮음 — 참조처가 전부 스킬 **이름**으로만 인용 |
| `skills/review-board/SKILL.md` 의 `~/bin/review_board_publish.sh`·`httpd_threaded.py` 운영 절 | 같은 곳 | 낮음 — 생성기 `tools/review_board_gen.py` 는 리포 안이라 팀원도 돈다 |
| `skills/code-review/SKILL.md` "`.51` 은 내 PR 도 타인 PR 로" | `members/정소희/README.md` (공통-CLAUDE.md 에도 있어 3중 기재) | 낮음 |
| `harness/pipelines/*.yaml` 의 `executor: ".51"` | 필드 삭제 또는 `local` | 중간 — 소비처가 없는 문서용인지 먼저 확인 |
| `harness/pipelines/*.yaml`·`pipeline.py` 의 `reviewer: soheejung-cs` 기본값 | `roster.json` 에서 읽기 | **중간 — `episodic.py` 가 이 값으로 게시 코멘트를 거른다. roster fallback 을 먼저 넣고 기본값을 뺀다** |
| `harness/templates/leak/*.sh` 의 `INSTALL:-/home/cubrid/release/CUBRID-expr-wt` | `INSTALL:?` 로 필수화 | 낮음 |
| `members/_조사메모-20260916-미확정.md` | 타인 이메일·스크래치패드 경로 삭제 후 `members/정소희/` 로 옮기거나 통째 삭제 | 낮음 — **판단은 사용자 몫**(매핑은 `roster.json` 이 이미 담당) |

### B. 개인 → 팀 (팀원이 못 여는 참조 끊기)

| 대상 | 가야 할 곳 | 위험 |
|---|---|---|
| `memory/rules/측정-규칙.md` §1·§2·§4·§5 | **정본을 팀 `rules/성능-리뷰-규칙.md` 로 못 박고** `review-testing` §3 은 절 번호만 인용 | **가장 조심할 곳** — 지금 `review-testing` §3 이 요약이 아니라 **재서술**이다(median/MAD·XASL·순서반전·계약 트랙 전부 중복). 먼저 어느 쪽이 정본인지 정하고, 재서술을 인용으로 바꾼다 |
| `memory/rules/JOB옵티마이저-규칙.md`("PG 가 레퍼런스, 순서 일치가 지표") | 팀 `rules/성능-리뷰-규칙.md` 판정 행으로 | 중간 — 같은 이유 |
| `tools/skill_link.sh` 사본 → 팀 `tools/skill_register.sh` | 팀 리포 `skills/*` 만 등록 | 낮음 — **지금 `팀-안내.md` 는 11개 중 `harness-review` 하나만 링크하라고 안내한다.** 팀원은 `code-review`·`review-response` 없이 하네스만 얻는다 |
| `tools/plan-ab/`(11파일) | 보류 — `env.sh` 의 경로 전제를 먼저 푼다 | 중간, 이번 범위 밖 |

### C. 중복 정리 — **손실 위험 있음, 먼저 머지**

`examples/리뷰보고서-예시-*.md` 13개가 개인 `projects/<JIRA>/` 와 겹친다.
**7쌍은 바이트 동일, 5쌍은 이미 갈라졌다** — `PR7658`·`PR7899`·`PR7937` 은 **팀이 최신**(게시 기록·근거 보강),
`PR7899-2차`·`PR7613` 은 **개인이 최신**(작성자 반영 확인 / 실측 조사).

절차: ① 갈라진 5건을 한쪽으로 머지 → ② 형식이 다른 3개만 예시로 남기고(`PR7899`, `PR7900-본문형식`, `PR7899-설계2차`) 나머지 삭제 → ③ 참조처 갱신
(`code-review/SKILL.md`, `review-response/SKILL.md`, `review_board_gen.py` 의 `DOCS`, `팀-안내.md`).
**하네스는 `examples/episodic/*.json` 만 읽으므로**(`context_pack.py`) `.md` 삭제는 하네스에 영향 없다.

## 하지 말 것

- `references/C-Cpp-성능규칙집.md` 를 개인으로 옮기지 않는다 — `~/.claude/성능규칙집.md` 가 **여기로의 심링크**다.
- 개인 `projects/CBRD-*/` 를 팀으로 옮기지 않는다 — 이슈별 산출물은 개인 소유이고, 팀이 필요로 하는 것은 그중 **예시 몇 개**뿐이다.
- `~/.claude/skills/synced/` 를 건드리지 않는다 — Claude Code 자체 번들(anthropic-skills)이고 `skill_link.sh` 와 무관하다.
