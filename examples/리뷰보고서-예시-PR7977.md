# PR #7977 자기 리뷰 보고서 — CBRD-27251

- PR: https://github.com/CUBRID/cubrid/pull/7977 (draft) · HEAD `a82e6d6ae` · 베이스 upstream/develop `6497720ec`
- 하네스: `/harness-review 7977` (.50, 2026-09-17) · 산출 `~/dev/utils/harness-out/7977/a82e6d6ae/` · `codegraph_complete=true`
- 성격: **자기 리뷰**(harness-implement S5) — 게시 대상 아님. 걸러낸 것은 같은 PR 에 반영.

## TL;DR

Non-blocking. valid 4 (blocking 0): 코드 🟢 1(주석) · 설계 🟢 1(본문 표) · 자동 MEAS 2(비해당). 코드 결함·관문 불균형·계층 간선 위반 없음. **두 지적 모두 반영**하고 draft 해제 승인 대기.

## 반영한 것

| # | 층·카테고리 | 지적 | 조치 |
|---|---|---|---|
| F-01 | 코드 · 주석 🟢 | `like_match_value`/`rlike_match_string` 의 "value 는 blob 안의 string_view" 주석이 재패딩(`pad_buf`) 뒤에는 틀림. 수명 결함은 없음(static: 즉시 `db_make_varchar`/`std::string` 복사) | 두 주석을 "blob 또는 pad_buf, 다음 호출까지만 유효, 아래서 복사" 로 정정 (후속 커밋) |
| F-02 | 설계 · 문서 🟢 | 수정 전/후 sel 표가 PR 본문에 없고 JIRA 첨부로만 | PR Remarks 에 9행 표 + `ignore_trailing_space=yes` 결과 추가 |

## 자동 finding(MEAS-01/05) 판정

PR 은 성능 개선을 주장하지 않는다(선택도 정확도 수정). MEAS-01 "측정 표" 는 sel 전/후 표로 갈음, MEAS-05 CTP 는 **요청자 확인 후**(review-testing) — 본문 DoD 의 "CI green" 이 그 자리다. 판정기 규칙상 valid 로 남지만 게시·조치 대상 아님.

## 리뷰 중 확인한 사실(변경 안 함)

- 컬럼 타입 기준 스트립은 호스트변수(런타임에 컬럼 도메인으로 강제변환됨)와도 정합 — VARCHAR 타입 호스트변수 vs CHAR 컬럼이 이제 MCV 에 맞는다.
- `INTL_CODESET_ASCII` 등 `intl_char_count` 가 다루지 않는 코드셋은 재패딩을 건너뛰어 현행 동작으로 폴백(보수적).
- `bind_fp_walk` 플랜 지문은 sel 을 재료로 쓰므로 값이 바뀔 수 있음 — 정확성 무관.

## 돌릴 것(제안)

CTP sql·medium(요청자 확인 후). 플랜이 바뀔 후보: VARCHAR 컬럼 + 후행공백 CHAR 리터럴, CHAR 컬럼 LIKE/REGEXP 후행공백 패턴.
