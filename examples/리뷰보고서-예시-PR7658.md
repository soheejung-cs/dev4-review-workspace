# PR #7658 코드 리뷰 보고서

**PR:** [CUBRID/cubrid#7658](https://github.com/CUBRID/cubrid/pull/7658)
**제목:** [CBRD-27215] Compile expression evaluation into step programs for aggregates and projections
**작성자:** soheejung-cs (이 컨테이너 규약: 내 PR 도 타인 PR 로 취급 -- 코드 수정 없이 코멘트만)
**HEAD SHA:** `9a13c297d` (mergeable: CONFLICTING)
**리뷰 일시:** 2026-09-16 (정소희 세션, Claude) -- 정적 리뷰, 8188줄 diff 전수. 앞선 리뷰 3차(HyunukLee 5 / xmilex-git / shparkcubrid 12)와 봇 지적은 HEAD 반영 여부만 확인하고 재지적하지 않았다.

> **TL;DR** (Non-blocking): 커널이 해석 경로를 미러한다는 주장은 앞선 리뷰 세 차례가 반례(단락·에러 순서·드리프트·40자리 반올림)를 찾아 좁혀 놓았고 HEAD 에 다 반영돼 있다. 남는 것은 (1) 성능 PR 인데 q7 +10% 회귀가 열려 있고 세 메커니즘이 한 트리라 원인을 수치로 가를 수 없다는 점, (2) SA 모드 `scan_prog_defer` 미초기화, (3) 원샷 XASL 의 `qexec_clear_xasl()` 이중 호출, (4) `is_final` 을 보지 않는 reset, (5) 도달 불가 NUMERIC 디코드 커널. 미응답 스레드 2건(깊이 가드·trace 노출)에는 구체안으로 답글.

## Summary

- **변경 요약**: regu 리스트를 실행당 1회 스텝 프로그램으로 컴파일(CSE·조건부 점프·프롤로그 호이스팅), 스캔 필터 컴파일, 필터→소비자 값 공유, 힙 고정폭 컬럼 디코드 플랜, NUMERIC 단일 워드 곱셈.
- **주요 이슈**: q7 회귀 미해결 + 메커니즘별 측정 부재(설계). 나머지는 한 줄 수정이나 확인 사항.
- **확인 필요 사항**: 커밋 단위 이분 측정 결과, 리베이스.

---

## Findings

### [설계 리뷰]

영역 판정: **[E] 서버 실행(query) + [G] 스토리지(heap 디코드)**. 클라이언트 변경 없음(프로그램은 서버 런타임 상태, 직렬화 안 됨) -- 1.2 유지. WAL·MVCC 무관. 공유 자원 계약은 "클론은 한 스레드에만 체크아웃" 한 줄에 의존하며 세 구조체 주석에 명시돼 있다(수용).

1. `cubrid/CMakeLists.txt:287` -- **세 메커니즘 한 PR**(식 프로그램 / 힙 디코드 플랜 / NUMERIC fixed64). TPC-H 수치는 합산이라 q7 1.10 의 귀속 불가. "한 변경 = 한 메커니즘 = 한 측정"(성능 규칙 §6). 이분 측정 결과 후 머지 권고. **MEAS-07**: 중앙값만 있고 MAD 없음.
2. 수명 설계(클론과 함께, 실행 끝에 슬롯만 비움)는 리뷰 2차 T9 의 "실행당 1회" 정리를 되돌린 것인데 근거(준비문 2000회 0.5 ms/문장)와 함께 적혀 있고, 다른 스레드에서 재사용될 때의 프라이빗 힙 문제를 reset 으로 닫았다. 채택.

### [코드 리뷰]

#### Non-blocking (should consider)

1. `src/parser/xasl_regu_alloc.cpp:270` -- SA 모드 `regu_init(pred_expr)` 가 `scan_prog_defer` 를 세우지 않는다(`stx_build_pred_expr()` 는 4필드). `pt_alloc_packing_buf()` 는 0 으로 채우지 않으므로 SA 에서 지연 컴파일 판정이 쓰레기 값으로 시작. 결과 동일, 커버리지만 비결정적.
2. `src/query/query_manager.c:1274` -- 원샷 XASL 에서 이미 `is_final=true` 로 비운 트리에 `XASL_DECACHE_CLONE` 을 세워 `qexec_clear_xasl()` 을 통째로 재호출. 가드 없음(NULL 검사만). 프로그램 3종만 해제하는 전용 워커 또는 플랜 캐시 OFF CTP 확인.
3. `src/query/query_executor.c:1829` -- `qexec_clear_pred()` 의 reset 이 `is_final` 을 보지 않아 상관 서브쿼리에서 바깥 행마다 프롤로그·서명 검사 재실행. `qexec_clear_agg_list()`·`qdata_release_valptr_list_prog()` 호출부도 동일.
4. `src/storage/heap_file.c:10544` -- `heap_attr_readval_numeric` 은 도달 불가(`tp_Numeric` `variable_p=1` → `is_fixed==0`, shparkcubrid 커버리지 실측). 80줄 수동 헤더 파서가 테스트 없이 들어감 → 제거 또는 "도달 불가" 명시.

#### 미응답 스레드 답글
- `expr_compile.c:2696` 깊이 가드 부재 -- 동의 + `expr_compile_node()` 에 depth 전달, limit 초과 시 -1 로 루트 거절(해석 경로가 같은 에러를 냄).
- `query_dump.c` trace 노출 -- 요약 한 줄은 기본, 스텝 리스팅은 시스템 파라미터 뒤로.

확인한 것(지적 없음): `posix_memalign` + `free_and_init` 짝(`memory_wrapper.hpp` 는 operator new/delete 만 감쌈), 점프 대상 경계 remap(`remap_boundary`), `expr_build_insert_step` 의 인덱스 이동, `expr_new_step_with_cell` 동시 확보, 루트 거절 되감기, 리터럴 CSE 의 NUMERIC 부호, 커뮤테이션 CSE 의 lazy 제외, 프롤로그 호이스팅의 에러 순서 보존(`expr_step_hoist`), `OR_ADD/SUB_OVERFLOW` 전환(해석 경로 UB 제거), scan pred leaf 드리프트 가드(`TYPE_CONSTANT` 면제 제거 반영), 정렬 GROUP BY 차원 리스트 초기화·해제 짝, px 워커 클론 `XASL_DECACHE_CLONE` 세팅, `HEAP_REC_LAYOUT` 의 OR_VAR_LENGTH 미러, `heap_attrinfo_start_with_index` `rd_attrepr` 초기화 반영, 리뷰 1~3차 수정 12건 HEAD 반영.

## Existing Comments

미해결 표시 12스레드 중 10건은 작성자 수정 후 resolve 만 안 된 것, 2건(깊이 가드·trace) 미응답 → 답글.

## 돌릴 것 (제안 -- 확인 후 실행)

**실행기 유형**: TPC-H 는 작성자 SSOT 측정 있음. (필수) q7 이분 측정은 작성자 몫. (추가) 플랜 캐시 OFF 상태 CTP sql 코어 여부(원샷 XASL 경로), SA 모드 CTP `_26_2h` (scan_prog_defer). JOB 은 회귀 유무만(스크리닝 1회).

## 게시 기록

2026-09-16 인라인 5건 + 스레드 답글 2건: https://github.com/CUBRID/cubrid/pull/7658#pullrequestreview-5221602586
