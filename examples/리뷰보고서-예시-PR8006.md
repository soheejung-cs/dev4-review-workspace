# PR #8006 자기 리뷰 보고서 — [CBRD-27143] LIKE / NOT LIKE 패턴 host variable 을 bind 지문에 포함

**HEAD** `3d0148e41` → 후속 `e225d238f` · 하네스 산출 `~/dev/utils/harness-out/8006/3d0148e41/` · `codegraph_complete=false`(ctags 폴백, 팩에 코드 발췌 없음 — 판정 근거는 세션에서 읽은 함수 본문)

**TL;DR**: 머지 차단 없음. 🟢 3건 — 주석 1(반영), TC 경계 1(반영, 동적 확인에서 기대 정정), 확인 질문 1(Remarks 에 기록).

## [설계 리뷰]
이 변경은 [B] SQL 컴파일(클라이언트측) 영역이고 불변조건 INV-1~5 를 흔들지 않는다. 격리 자원(세션 kept 서브세션)만 건드린다. 후보 집합의 정의를 "플래너가 값으로 가격 매기는 술어" 에 맞춘 것이 설계 판단의 핵심이며 ESCAPE 형·역순을 제외한 이유가 그것이다.

## [코드 리뷰]
1. 🟢 [주석 제안] `histogram_cl.cpp` LIKE 밴드 성분 `(std::uint64_t)(band + 1024)` — 서브노멀 입력이면 음수를 지날 수 있는 캐스트인데 도달 불가 근거가 없었다. static: 추정기가 sel 을 1/N 로 하한, NOT LIKE 의 1-sel 은 0 또는 정규수 → 지수 ≥ 약 -53. 결함 아님(모듈로 변환은 정의된 동작, 지문 결정적). **반영** e225d238f.
2. 🟢 [테스트 제안] NULL·비문자열 패턴 경계 TC 없음. **반영** `[3-1]` 절. 동적 확인 결과 기대 정정: NULL 은 가격 불가 → 재플랜 없음(0행); 정수 123 은 `do_cast_host_variables_to_expected_domain` 으로 컬럼 문자열 도메인이 되어 **가격 매겨져 재플랜**(0행, 오류 없음).
3. 🟢 [확인 질문] `hostvar_peeking=yes`(히든, 기본 no) + NOT NULL 컬럼이면 기존 `LIKE_RECOMPILE_CANDIDATE` 전면 재컴파일(db_vdb.c:3137)과 첫 실행 peek(3778) 가 같은 EXECUTE 에 겹쳐 플랜 2회. 비용만의 문제, 기본 설정 비발생. 동적 미확인 → PR Remarks 에 질문으로 기록.

## 돌릴 것(제안)
CTP sql/medium 전체(요청자 확인 후). JOB/TPC-H 불필요(host variable 경로 전용).
