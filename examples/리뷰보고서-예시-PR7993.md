# PR #7993 자기 리뷰 보고서 — CBRD-27126 비용 단가 파라미터화

**PR:** https://github.com/CUBRID/cubrid/pull/7993 · **HEAD** `06b647fc9` · **기준** upstream/develop `4f0f90b62` · 하네스 `e9718fe`, codegraph complete
**판정:** 🟡 이하 5건, 🔴 0, 코드 수정 0 — 게시 대상 없음(자기 PR, 본문에 반영). 산출물 `~/dev/utils/harness-out/7993/06b647fc9/`

## 영역 판정
이 변경은 [B] SQL 컴파일(옵티마이저 비용 모델) + [I] 시스템 파라미터 정의 영역이고, INV-1~5 를 흔들지 않는다. INV-2 는 오히려 설계의 근거(`PRM_FOR_CLIENT`, 서버 왕복 없음). INV-3 은 `prm_Def[]` 공용 편집으로 touched — SA/CS/서버 세 모드 컴파일과 SA·CS 실행을 동적으로 확인했다. 관문 경로 8홉 내 없음. Tier-1. 전역 가변 상태 신설 없음.

## 지적 5건

| # | 층·카테고리 | 중요도 | 내용 | 조치 |
|---|---|---|---|---|
| F2 | 코드·테스트 제안 | 🟡 | 비용 모델 전 경로를 건드렸는데 CTP sql/medium 근거 없음(MEAS-05). 같은 PR 안에 float 표현 하나로 NL 덤프 58→57 이 흔들린 전례가 있어 6질의 덤프 비교만으로 무회귀 확정 불가 | CI 또는 로컬 CTP → Remarks 갱신(요청자 확인 후) |
| F1 | 코드·측정 요청 | 🟢 | 단가 매크로가 상수 → 파라미터 읽기(+CPU 는 1.0/400 나눗셈)로 바뀌어 비용 함수마다 ≤15회 추가. develop 이 `PRM_ID_SR_NBUFFERS` 등을 같은 식으로 읽고 있어 코드만으로 '느려진다' 단정 불가(MEAS-01) | 17~25 테이블 조인 컴파일 시간 median-of-5 로 측정 가능; MAD 밖이면 함수 진입 시 지역 복사(GLOB-01) |
| F3 | 코드·확인 질문 | 🟢 | 튠 구간 검증이 전부 `-S` 였음 — CS 모드에서 `SET SYSTEM PARAMETERS` 가 클라이언트 파라미터에 적용되는가 | **동적 확인됨**: 서버 기동 후 CS csql 로 헤더 8줄·거부 4건·`random_page 4` 반영 |
| F4 | 설계·문서 제안 | 🟢 | `cost_seq_page` 가 단위 앵커라 `cost_cpu_tuples_per_page` 와 효과 부분 중복 — 사용 지침(1.0 고정, 비율로 튠) 이 본문에 없음 | 본문에 한 줄 추가(반영) |
| F5 | 코드·테스트 제안 | 🟢 | 헤더 출현/소멸·범위 밖 거부를 고정하는 sql TC 없음 — 비용 숫자와 무관한 안정 계약 | 발번은 사용자 결정 |

## 검증 기록
- 같은 demodb 볼륨에 두 바이너리(`-S`)를 붙여 기본값 5플랜 덤프 182줄 바이트 동일(NL 58=58). 첫 구현(float 0.0025f)은 58→57 로 어긋났고 정수 역수(400)로 해소.
- 튠 효과: random×4 → iscan 2→8, sscan 607 불변 / random×2 비커버링 NL 1만 프로브 18,326→18,685(포화 357 페이지 ×2) / cache=0 → 1,017,969(포화 해제, NaN 없음) / cpu 단가 ×10 → sscan 607→2,857 / 범위 밖 4건 거부.
- 재현 SQL·스크립트: `~/dev/scratch/cbrd27126/{cbrd27126_repro.sql,verify.sh}`.

## 돌릴 것(제안)
CTP sql·medium(F2). JOB/TPC-H 는 기본값 무영향이라 불필요.
