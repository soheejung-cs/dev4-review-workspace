# PR 7937 하네스 2차(보완) — PG costsize.c 대조 4건 (2026-09-17)

기존 4건 코멘트(…review-5221471419)와 중복 없음. 게시 전 초안. 산출: `harness-out/7937/02f49316d/{findings.json,findings.adjudicated.json,drafts.json}`.

# PR #7937 리뷰 보고서 (하네스 생성 골격)

**PR:** https://github.com/CUBRID/cubrid/pull/7937  **HEAD:** `02f49316d`  **JIRA:** CBRD-27186

> **TL;DR** (Non-blocking): valid 6 (blocking 0) · inconclusive/invalid 0 · requery 0

## Findings

### [설계 리뷰]

- [설계 리뷰] [측정 요청] 🟡 `src/optimizer/query_graph.c:6133` — 성능 변경인데 기능 회귀 테스트(CTP) 통과 근거가 본문에 없다
  - 왜 문제인가: 정확성이 성능보다 먼저다 — 회귀 테스트 근거가 없으면 빨라진 코드가 틀린 답을 내는지 아무도 확인하지 않은 상태로 머지된다.
  - 제안: `/run all` 결과 또는 CTP sql/medium 실행 결과(코어 0, NOK 분류)를 본문 Verification 에 한 줄로. 예: "CTP sql 통과, medium NOK 2건은 기존 답안 차이".
  - status **valid**, non-blocking, rules ['MEAS-05']
- [설계 리뷰] [측정 요청] 🟡 `src/optimizer/query_graph.c:6133` — 중앙값만 있고 산포(MAD/표준편차/rep 별 값)가 없다 — 개선폭이 잡음 범위 안인지 판정 불가
  - 왜 문제인가: 산포 없이는 0.9 배 개선과 1.1 배 회귀가 같은 잡음 폭 안일 수 있어, 표의 방향 자체를 믿을 수 없다.
  - 제안: 표에 MAD(또는 표준편차/rep 별 값) 열을 추가한다. 예: `| q7 | 7.04 (MAD 0.05) | 7.75 (MAD 0.07) | 1.10 |` — 산포가 있어야 1.10 이 잡음 밖임이 보인다.
  - status **valid**, non-blocking, rules ['MEAS-07']
- [설계 리뷰] [설계 판정] 🟡 `src/optimizer/query_graph.c:10567` — [기존 스레드 보완] PG 는 FK 바닥을 nullable 여부와 무관하게 적용하고 null 보정을 하지 않는다(costsize.c get_foreign_key_join_selectivity: 'XXX ... we do nothing for now about correcting for nulls') — 이유는 (1) 같은 컬럼에 strict 제한 절이 있으면 null 비율을 두 번 깎게 되고 (2) 여러 참조 컬럼의 null 비율을 합치는 방법이 불분명해서다. CUBRID PR 은 반대 극단(선언만 nullable 이면 전부 포기)이고, 제가 앞서 제안한 (1-nf) 보정은 PG 가 피한 이중 차감 함정을 안고 있다
  - 왜 문제인가: 세 선택지의 오차 방향이 다르다: 전부 포기(현 PR)는 적용 범위를 잃어 27186 의 과소 추정이 그대로 남고, 무조건 적용(PG)은 null 이 많은 FK 컬럼에서 과대 추정, (1-nf)는 같은 컬럼에 IS NOT NULL/등호 제한이 있을 때 이중 차감. 어느 것을 택하든 근거를 적어야 다음 사람이 뒤집지 않는다.
  - 제안: PG 규칙 채택을 권한다 — nullable 도 등록하되 주석에 PG 의 두 이유를 옮겨 적고, null 보정은 '그 FK 컬럼에 strict 제한 절(IS NOT NULL·등호)이 없을 때만 max(nf) 로 한 번' 으로 한정하면 이중 차감을 피한다. 코드: qo_match_fk_prefix() 의 `if (!QO_SEG_IS_NOT_NULL (fk_seg)) break;` 를 `nullable_seen = true;` 로 바꾸고, 등록 시 `floor = (has_strict_restriction ? 1.0 : (1.0 - nf_max)) / NCARD(pk)`. TC: nullable FK + IS NOT NULL 필터 조합 플랜 답안.
  - 검증: [static] postgres-16 costsize.c:5302~ get_foreign_key_join_selectivity 주석 확인('we do nothing for now about correcting for nulls', 이유 2개); Oracle 은 FK 를 조인 카디널리티에 쓰지 않고 join elimination 에 쓴다(Tuning Guide 미보유 — 매뉴얼 미확인)
  - status **valid**, non-blocking, rules -
- [설계 리뷰] [확인 질문] 🟡 `src/optimizer/query_planner.c:8257` — 바닥이 조인 종류를 보지 않는다. PG 는 semi/anti 조인에서 참조 테이블이 안쪽(inner)일 때만 rows/tuples 로 적용하고 바깥쪽이면 FK 를 무시한다(바깥 행 중 매칭 비율을 FK 가 말해 주지 못함). 여기서는 outer join 하한(8257-8268, 8418-8429)만 있고 semi/anti(IN/EXISTS 언네스트) 경로의 바닥 처리가 보이지 않는다
  - 왜 문제인가: pk 쪽이 바깥인 세미 조인(예: pk_node 가 구동 테이블이고 fk 쪽이 EXISTS 서브쿼리)에 1/N(pk) 바닥을 그대로 곱하면 '바깥 행 하나당 안쪽 매칭 최대 1' 이라는 세미 조인 의미와 어긋나 과소 추정된다. 팩에 그 경로가 없어 실제 동작은 확인하지 못했다.
  - 제안: planner_visit_node 가 semi/anti 조인 타입을 구분한다면 그 경우 (a) 참조 테이블이 안쪽일 때만 `ref_rows/ref_tuples` 로, (b) 바깥쪽이면 바닥을 건너뛴다(PG costsize.c 규칙 그대로). 구분하지 않는다면 어떤 경로로 EXISTS/IN 이 오는지 한 줄 답변 부탁. TC: `SELECT count(*) FROM p WHERE EXISTS (SELECT 1 FROM f WHERE f.a=p.a AND f.b=p.b)` 카디널리티가 |p| 부근인지.
  - 검증: [static] postgres-16 costsize.c:5302~ semi/anti 분기(ref_is_outer → continue; inner → rows/tuples) 확인; CUBRID 세미 조인 경로는 팩 밖 → 확인 질문
  - status **valid**, question, rules -
- [설계 리뷰] [설계 판정] 🟡 `src/optimizer/query_planner.c:8362` — 같은 (fk_node, pk_node) 쌍을 잇는 FK 제약이 둘이면 두 바닥을 곱한다(fk_floor_product *= … 를 제약마다) → 1/N². PG 는 '두 FK 가 같은 EC 에 매칭되면 첫 FK 만 소비하고 나머지는 punt' 한다 — 독립 적용이 조인 크기를 과소 추정할 위험과 EC 가 중복 등호의 이중 계산을 막기 위해 있다는 점을 들어
  - 왜 문제인가: self-reference 복합 FK 두 개(m1,m2)→(k1,k2), (n1,n2)→(k1,k2) 를 같은 부모 alias 에 동시에 조인하면 실제 결과는 'm==n 인 행' 이라 1/N × P(m==n) 인데, 1/N² 은 두 FK 가 독립이라는 가정이다. 과소 추정은 27186 이 고치려는 방향과 반대이고, 본문 검증(TPC-H)에는 이 형상이 없다.
  - 제안: PG 처럼 같은 (fk_node, pk_node) 쌍에서는 첫 제약의 바닥만 적용하고 두 번째 제약의 term 은 제외 목록에 넣지 않은 채(일반 선택도 곱으로) 두거나, 최소한 두 번째부터는 바닥을 곱하지 않는다. 코드: `if (all_cols_found)` 안에서 (fk_node,pk_node) 쌍을 비트셋으로 기억해 두 번째부터 `fk_floor_product` 갱신을 건너뜀. TC: 위 self-reference 두 FK 케이스의 카디널리티가 1/N 부근인지.
  - 검증: [static] postgres-16 costsize.c:5302~ '2. The clauses were matched to some other FK … just punt, since this is a fairly uncommon situation' 확인; CUBRID 쪽은 팩의 8359-8363 에서 제약마다 곱함 확인. 실제 카디널리티 오차 크기는 재현 안 함 → question
  - status **valid**, question, rules -

### [코드 리뷰]

- [코드 리뷰] [주석 제안] 🟢 `src/optimizer/query_planner.c:8352` — FK 컬럼의 eqclass 가 상수를 포함하면(fk.a = 5 AND pk.a = 5 → 등호 조인 term 없음) 그 컬럼의 term 을 못 찾아 all_cols_found=false 로 바닥이 조용히 빠진다. PG 는 이 경우(nconst_ec)를 세어 바닥을 계속 적용하고 상수 컬럼만큼 보정한다
  - 왜 문제인가: 보수적(과소 적용)이라 오답은 아니지만, 흔한 형상(`WHERE fk.region = 'KR' AND pk.region = 'KR'`)에서 수정이 빠지는 이유가 코드에 적혀 있지 않아 나중에 '왜 안 걸리나' 를 다시 파야 한다.
  - 제안: 이 `if (!found)` 위에 주석 한 줄(suggestion): 상수 EC 컬럼은 term 이 없어 바닥을 건너뛰며, PG 의 nconst_ec 보정은 후속 과제라고. 여유가 있으면 후속 이슈로 '상수 EC 컬럼은 매칭된 것으로 간주(PG nconst_ec)'.
  - 검증: [static] postgres-16 costsize.c 'If the FK match is based on an EC that is ec_has_const, it won't have generated any join clauses at all. We discount such ECs' 확인; CUBRID 는 8352-8355 에서 found=false → 미적용 확인
  - status **valid**, non-blocking, rules -

### 판정 보류 (requery.json)

없음

## 아키텍처 리스크 (결정론 탐지)

- [medium] gate-imbalance `src/optimizer/query_graph.c:qo_env_new` — alloc-free = +1 (이 함수 안의 호출 수 차; 에러 경로 unfix/unlock 누락 후보 — 조건 분기·호출자 위임이면 오탐)

## 돌릴 것 (제안 — 요청자 확인 후)

- 변경 계층 ['optimizer'] → review-testing 매트릭스로 CTP/동시성/JOB/TPC-H 제안

_manifest: harness 8789797, model unset_