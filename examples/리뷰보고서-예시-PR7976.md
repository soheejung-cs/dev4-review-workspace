# PR #7976 자기 리뷰 보고서 (harness-implement S5, 2026-09-17)

> 구현자 자신의 PR 을 `/harness-review` 로 돈 결과. 자동 MEAS-01/05 두 건은 본문에 성능 주장이 없는데 표의 행 수(3행/1행)·버퍼 크기(2048→4096) 숫자를 성능 수치로 본 **오탐** — 게시하지 않고 Remarks 에 'CTP sql(신규 TC 4건) 코어 0' 한 줄만 추가. 🟢 두 건 중 CASE 주석은 커밋으로 반영, TC 1004 는 사용자 결정으로 TC 자체를 추가하지 않아 미반영.


**PR:** https://github.com/CUBRID/cubrid/pull/7976  **HEAD:** `d97b4b5b0`  **JIRA:** CBRD-27043

> **TL;DR** (Non-blocking): valid 4 (blocking 0) · inconclusive/invalid 0 · requery 0

## Findings

### [설계 리뷰]

- [설계 리뷰] [측정 요청] 🟡 `src/object/schema_system_catalog_install.cpp:2223` — 성능 변경인데 기능 회귀 테스트(CTP) 통과 근거가 본문에 없다
  - 왜 문제인가: 정확성이 성능보다 먼저다 — 회귀 테스트 근거가 없으면 빨라진 코드가 틀린 답을 내는지 아무도 확인하지 않은 상태로 머지된다.
  - 제안: `/run all` 결과 또는 CTP sql/medium 실행 결과(코어 0, NOK 분류)를 본문 Verification 에 한 줄로. 예: "CTP sql 통과, medium NOK 2건은 기존 답안 차이".
  - status **valid**, non-blocking, rules ['MEAS-05']
- [설계 리뷰] [측정 요청] 🟡 `src/object/schema_system_catalog_install.cpp:2223` — 성능 주장은 있는데 측정 표(타이밍/프로파일)가 본문에 없다 — 측정 먼저
  - 왜 문제인가: 측정 없는 성능 주장은 머지 뒤 회귀가 나도 기준선이 없어 원인을 되짚을 수 없고, 리뷰어가 코드만 읽고 "빨라 보인다"에 동의하는 것은 근거가 아니다.
  - 제안: 본문에 측정 표를 추가: 워크로드·계약(버퍼/병렬도/핀)·warmup·반복 수·median·MAD. 예: `| q1 | before 32.2s (MAD 0.3) | after 15.3s (MAD 0.2) | 0.47 |`
  - status **valid**, non-blocking, rules ['MEAS-01']

### [코드 리뷰]

- [코드 리뷰] [주석 제안] 🟢 `src/object/schema_system_catalog_install_query_spec.cpp:1781` — CASE [h].[with_fullscan] WHEN 0 … ELSE 'FULL SCAN' 은 with_fullscan 이 NULL 이어도 'FULL SCAN' 을 낸다 — 원 스펙(= 0 비교)과 같은 동작이지만 의도가 주석에 없다.
  - 왜 문제인가: smt_add_histogram 이 항상 int 를 넣어 현재 NULL 은 생기지 않으나, 뷰 리터럴이 사실('FULL SCAN')을 단정하는 자리라 NULL 을 기본값으로 접는 이유가 코드에 없으면 다음 사람이 ELSE 를 NULL 로 바꿔도 되는지 판단할 근거가 없다. 동작 변화는 없고 표기 문제라 낮음.
  - 제안: ```suggestion
	  "CASE [h].[with_fullscan] WHEN 0 THEN 'SAMPLING SCAN' ELSE 'FULL SCAN' END AS [scan_type], " /* with_fullscan is a non-null int written by smt_add_histogram; ELSE covers only 1 */
```
  - 검증: [static] smt_add_histogram (schema_template.c:2114) 이 db_make_int (&value, with_fullscan) 으로 항상 값을 넣고, 컬럼 정의(get_histogram) 도 integer 라 NULL 경로 없음 — 동작 결함 아님
  - status **valid**, non-blocking, rules -
- [코드 리뷰] [테스트 제안] 🟢 `src/object/schema_system_catalog_install_query_spec.cpp:1800` — 권한 필터의 세 갈래 중 '사용자가 속한 그룹이 소유자/피부여자인 경우' 는 TC(1001·1002)가 덮지 않는다 — 그룹 경로가 깨져도 CTP 가 잡지 못한다.
  - 왜 문제인가: 필터는 CURRENT_USER 와 그 그룹 집합(SET {CURRENT_USER} + SUM (SET {[t].[g].[name]}))을 함께 비교하는데 TC 는 직접 사용자만 시험한다. db_index 와 같은 관용구를 복사했으므로 지금은 맞지만, 이 뷰만 나중에 고칠 때 그룹 갈래가 빠져도 답안이 그대로 통과한다(발견 가능성 낮음). 권한 뷰의 회귀는 값 노출로 이어지므로 세 갈래 모두 답안에 박아 두는 편이 안전하다.
  - 제안: TC 1004 추가: dba 가 그룹 사용자 g_hist 를 만들고 u_member 를 ADD MEMBER 로 넣은 뒤 g_hist 에게만 GRANT SELECT ON t_hist_grp → u_member 로 로그인해 SELECT … FROM db_histogram WHERE class_name = 't_hist_grp' 가 1행(k) 이어야 한다. 소유자-그룹 갈래는 g_hist 가 소유한 테이블을 u_member 가 보는 시나리오로 한 건 더.
  - status **valid**, non-blocking, rules -

### 판정 보류 (requery.json)

없음

## 아키텍처 리스크 (결정론 탐지)


## 돌릴 것 (제안 — 요청자 확인 후)

- 변경 계층 ['object'] → review-testing 매트릭스로 CTP/동시성/JOB/TPC-H 제안

_manifest: harness 0a56ed5, model unset_