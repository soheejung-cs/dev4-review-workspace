# PR #7900 자기 리뷰 보고서 — 재설계 후 (2026-09-17, .50)

**HEAD:** `8e05f8386` (합성 OID 게이트 폐기 → `_db_class` 행 게이트 + writer X-first). 하네스 팩은 .50 에 tree-sitter 가 없어 ctags 폴백(시그니처만) — 지적은 diff 정독 + 동적 재현 근거. 판정 valid 5 / blocking 0 / requery 0. 게시 대상 아님(자기 리뷰): F1·F2 는 커밋으로 반영, F3 은 PR Remarks 에 기재, F4 는 본문 한 줄, MEAS-05 는 CI `/run all` 로.

# PR #7900 리뷰 보고서 (하네스 생성 골격)

**PR:** https://github.com/CUBRID/cubrid/pull/7900  **HEAD:** `8e05f8386`  **JIRA:** CBRD-27369

> **TL;DR** (Non-blocking): valid 5 (blocking 0) · inconclusive/invalid 0 · requery 0

## Findings

### [설계 리뷰]

- [설계 리뷰] [측정 요청] 🟡 `src/communication/network_interface_cl.c:6156` — 성능 변경인데 기능 회귀 테스트(CTP) 통과 근거가 본문에 없다
  - 왜 문제인가: 정확성이 성능보다 먼저다 — 회귀 테스트 근거가 없으면 빨라진 코드가 틀린 답을 내는지 아무도 확인하지 않은 상태로 머지된다.
  - 제안: `/run all` 결과 또는 CTP sql/medium 실행 결과(코어 0, NOK 분류)를 본문 Verification 에 한 줄로. 예: "CTP sql 통과, medium NOK 2건은 기존 답안 차이".
  - status **valid**, non-blocking, rules ['MEAS-05']
- [설계 리뷰] [확인 질문] 🟢 `src/object/schema_template.c:1976` — 게이트를 거치지 않는 UPDATE STATISTICS ON ALL CLASSES 에서 아직 히스토그램 행이 없는 컬럼을 두 세션이 동시에 첫 수집하면, committed 존재확인이 상대의 미커밋 삽입을 못 보고 둘 다 INSERT 를 시도한다 — 유니크 인덱스 u__db_histogram_class_of_key_attr 가 있어 중복 행은 생기지 않지만, 두 번째 세션이 유니크 실패로 문장 에러를 받을 수 있다.
  - 왜 문제인가: 수정 전 존재확인은 S 락 조회라 상대의 미커밋 삽입 행에서 대기한 뒤 '존재' 로 봤다. 이제 그 대기가 없어 첫 수집 경합에서 성공→에러로 바뀔 가능성이 있다. 명시 ON <table> 경로는 게이트가 직렬화하므로 해당 없음; ALL CLASSES 첫 수집 동시 실행이라는 드문 관리 시나리오에 한정.
  - 제안: (a) 수용: ALL CLASSES 게이트 확장(Q3) 때 함께 닫힌다고 Remarks 에 한 줄. (b) 지금 닫으려면 smt_add_histogram 의 INSERT 가 ER_BTREE_UNIQUE_FAILED 를 받으면 ER_LC_CLASSNAME_EXIST 로 바꿔 '이미 있음' 으로 진행. TC: 히스토그램 없는 새 테이블 2개 + 세션 4개 ALL CLASSES 동시 → 전건 성공, 행 수 = 컬럼 수.
  - 검증: [dynamic] release 8e05f8386, demodb: 매 실행 새로 만든 ta1/ta2(히스토그램 없음)에 8세션×5회 ALL CLASSES 동시 실행 2회 → 40/40 성공·데드락 0, _db_histogram 중복 행 0(GROUP BY class_of,key_attr HAVING COUNT>1 = 0건). 유니크 실패는 재현되지 않았으나 창은 코드상 존재한다.
  - status **valid**, question, rules -

### [코드 리뷰]

- [코드 리뷰] [주석 제안] 🟢 `src/object/object_accessor.c:3691` — obj_find_multi_attr_internal () 헤더의 'Work function of obj_find_multi_attr () and obj_find_multi_attr_committed ()' 에 obj_find_multi_attr_for_update () 가 빠졌다.
  - 왜 문제인가: 세 진입점의 락 모드 차이(S / 없음 / X)가 이 함수 한 곳에 모여 있는데 목록이 불완전하면 for_update 분기가 어디서 오는지 추적이 끊긴다.
  - 제안: ```suggestion
 * obj_find_multi_attr_internal - Work function of obj_find_multi_attr (), obj_find_multi_attr_committed ()
 *                                and obj_find_multi_attr_for_update ()
```
  - status **valid**, non-blocking, rules -
- [코드 리뷰] [주석 제안] 🟢 `src/optimizer/histogram/histogram_cl.cpp:2268` — db_get_histogram_committed () 헤더 주석의 'Writers, which need the current row, keep using db_get_histogram ()' 가 더는 사실이 아니다 — writer 는 이제 db_find_multi_unique_committed (존재확인) / db_find_multi_unique_for_update (store·drop) 를 쓴다.
  - 왜 문제인가: 다음 리뷰어가 이 주석을 믿고 db_get_histogram () 을 writer 진입점으로 다시 쓰면 S→X 승급이 되살아난다(이 PR 이 없앤 데드락의 재도입 경로). 주석은 락 규약을 전달하는 유일한 자리다.
  - 제안: ```suggestion
 *   reader, e.g. the optimizer's histogram read (CBRD-27369). Writers take the row X at the lookup instead
 *   (db_find_multi_unique_for_update ()); the existence check reads committed, unlocked (db_find_multi_unique_committed ()).
```
  - status **valid**, question, rules -; 에러 우려인데 verification(static/dynamic 확인 결과) 없음 → severity 를 question 으로 내림
- [코드 리뷰] [문서 제안] 🟢 `src/storage/statistics_sr.c:141` — xstats_enter_update_gate () 에서 heap_get_class_name () 실패가 이제 '편승 포기' 가 아니라 문장 에러가 된다(게이트 키가 클래스명이라 불가피).
  - 왜 문제인가: 동작 변화 자체는 맞다(이름을 못 얻는 클래스는 뒤의 stats_update_statistics_internal 도 같은 이유로 실패한다) — 다만 이전 라운드 리뷰어가 본 '실패해도 진행' 계약과 달라졐으니 주석/본문에 명시돼 있어야 오해가 없다.
  - 제안: 이미 코드 주석에 'failing to resolve it is fatal' 이 있다. PR 본문 Implementation (A) 항에 '클래스명 조회 실패는 문장 에러(게이트 자원이 클래스명으로 결정됨)' 한 줄 추가.
  - 검증: [static] stats_update_statistics_internal () 도 heap_get_class_name 실패 시 ASSERT_ERROR_AND_SET 후 반환 — 같은 조건에서 어차피 실패하므로 관측 가능한 차이는 에러가 나는 지점만.
  - status **valid**, non-blocking, rules -

### 판정 보류 (requery.json)

없음

## 아키텍처 리스크 (결정론 탐지)


## 돌릴 것 (제안 — 요청자 확인 후)

- 변경 계층 ['communication', 'compat', 'object', 'optimizer', 'storage', 'transaction'] → review-testing 매트릭스로 CTP/동시성/JOB/TPC-H 제안

_manifest: harness 8846efc, model unset_